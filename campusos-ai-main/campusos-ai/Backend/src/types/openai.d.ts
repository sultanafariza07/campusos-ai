// Minimal types needed for fetch JSON parsing in TS strict mode.
// This project runs in Node and relies on the global fetch (Node 18+).
// If the build fails due to fetch typings, replace this with proper @types/node-fetch.

export {}

